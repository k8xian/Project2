<div class="info__bar">
    <div class="info__inside">
        <a class="title__option leaderboard">Mission no: {{id}} - {{missionName}}</a>
        <div class="final__message">{{victoryMessage}}{{epitaph}}</div>
    </div>
</div>